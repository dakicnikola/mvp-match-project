import {pathname_settings} from '../../config/routes'

const Settings = () => {
  return (
    <div>
      Settings
    </div>
  )
}


const settings = {
  path: pathname_settings,
  element: <Settings />,
}

export {settings}