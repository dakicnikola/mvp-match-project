import {pathname_board} from '../../config/routes'

const Board = () => {
  return (
    <div>
      Board
    </div>
  )
}

const board = {
  path: pathname_board,
  element: <Board />,
}

export {board}
